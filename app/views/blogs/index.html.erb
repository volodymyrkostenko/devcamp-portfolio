

      <div class="col-8 blog-main">
      	<%= link_to 'New Blog', new_blog_path, class: "btn btn-primary btn-lg btn-block" if logged_in?(:site_admin)   %>
         <%= paginate @blogs %>
        <%=render @blogs %>
        <%= paginate @blogs %>
      </div>
      
    
<br>


 